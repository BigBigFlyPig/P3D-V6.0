namespace AIDestroyer
{
    /// <summary>
    /// The different request types we are
    /// makeing in this SimConnect App.
    /// These are used as IDs for requests being sent
    /// to and from simconnect.
    /// </summary>
    enum DataIdentifier
    {
        RetreiveSimObjects = 0,
        RetreiveHealthInfo = 1
    }
}